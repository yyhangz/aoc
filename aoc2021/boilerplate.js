const reader = require('./reader');
const fname = 'dayXX.in';

const part1 = async () => {
    const input = await reader.getContents(fname);
}

const part2 = async () => {
    const input = await reader.getContents(fname);
}

part1().then(result => console.log(result));
part2().then(result => console.log(result));
